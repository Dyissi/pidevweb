
parameters:
    # Add any parameters you want to use in your services here
    api_ninjas_key: '%env(API_NINJAS_KEY)%'

services:
    _defaults:
        autowire: true
        autoconfigure: true

    Nucleos\DompdfBundle\Factory\DompdfFactoryInterface:
        class: Nucleos\DompdfBundle\Factory\DompdfFactory
        arguments:
            $options: { }

    # Makes classes in src/ available to be used as services
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    # Twilio Service
    App\Service\TwilioService:
        arguments:
            $sid: '%env(TWILIO_SID)%'
            $token: '%env(TWILIO_AUTH_TOKEN)%'

    # Explicit Finder service configuration
    Symfony\Component\Finder\Finder:
        class: Symfony\Component\Finder\Finder
        shared: false

    # Google calendar service 
    App\Service\GoogleCalendarService:
        arguments:
            $clientId: '%env(GOOGLE_CLIENT_ID)%'
            $clientSecret: '%env(GOOGLE_CLIENT_SECRET)%'
            $redirectUri: '%env(GOOGLE_REDIRECT_URI)%'
            $logger: '@logger'

<<<<<<< HEAD
  
=======
    # QuickChart service configuration
    App\Service\QuickChartService:
        arguments:
            $httpClient: '@http_client'

    # WgerApiService configuration
    App\Service\WgerApiService:
        arguments:
            $httpClient: '@http_client'
            $apiKey: '%api_ninjas_key%'
>>>>>>> e7c17639e2d7249c7c164c027174f32c77fb3d02
