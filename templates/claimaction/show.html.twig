{% extends 'baseB.html.twig' %}

{% block title %}Claim Action{% endblock %}

{% block main %}
    <div class="card-header">
        <i class="fas fa-table me-1"></i>
        Claim Action N°{{ claimaction.claimActionId }}
    </div>

    <div class="card-body">
        <table class="table table-striped mt-3">
            <tbody>
                <tr>
                    <th>Action Type</th>
                    <td>{{ claimaction.claimActionType }}</td>
                </tr>
                <tr>
                    <th>Start Date</th>
                    <td>{{ claimaction.claimActionStartDate ? claimaction.claimActionStartDate|date('Y-m-d') : '' }}</td>
                </tr>
                <tr>
                    <th>End Date</th>
                    <td>{{ claimaction.claimActionEndDate ? claimaction.claimActionEndDate|date('Y-m-d') : '' }}</td>
                </tr>
                <tr>
                    <th>Notes</th>
                    <td>{{ claimaction.claimActionNotes }}</td>
                </tr>
                <tr>
                    <th>Related Claim</th>
                    <td>
                        {{ claimaction.claim ? claimaction.claim.claimDescription : 'N/A' }}
                    </td>
                </tr>
            </tbody>
        </table>

        <div class="d-flex justify-content-between mt-4">
            <a href="{{ path('app_claimaction_index') }}" class="link-secondary">Back to list</a>
            <a href="{{ path('app_claimaction_edit', {'claimActionId': claimaction.claimActionId}) }}">Edit</a>
            {{ include('claimaction/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
