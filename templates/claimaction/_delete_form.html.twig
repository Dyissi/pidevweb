<form method="post" action="{{ path('app_claimaction_delete', {'claimActionId': claimaction.claimActionId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ claimaction.claimActionId) }}">
    <button class="btn">Delete</button>
</form>
