{% extends 'base.html.twig' %}

{% block title %}Results{% endblock %}

{% block body %}
    <h1>Results</h1>

    <table class="table">
        <tbody>
        </tbody>
    </table>

    <a href="{{ path('app_results_index') }}">back to list</a>

    <a href="{{ path('app_results_edit', {'tournament': result.tournament}) }}">edit</a>

    {{ include('results/_delete_form.html.twig') }}
{% endblock %}
