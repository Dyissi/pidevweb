<form method="post" action="{{ path('app_results_delete', {'tournament': result.tournament}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ result.tournament) }}">
    <button class="btn">Delete</button>
</form>
