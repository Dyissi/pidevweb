{% extends 'baseB.html.twig' %}

{% block title %}
  Edit {% if role == 'coach' %}Coach{% elseif role == 'med_staff' %}Medical Staff{% else %}Athlete{% endif %} {{ user.userFname }} {{ user.userLname }} | SPIIN Sports
{% endblock %}
{% block description %}
  Edit {{ user.userFname }} {{ user.userLname }} in the SPIIN Sports system
{% endblock %}
{% block keywords %}
  edit {% if role == 'coach' %}coach{% elseif role == 'med_staff' %}medical staff{% else %}athlete{% endif %}, {{ user.userFname }} {{ user.userLname }}, sports, SPIIN
{% endblock %}

{% block main %}
  <div class="container-fluid px-4">
    <h1 class="mt-4">Edit {% if role == 'coach' %}Coach{% elseif role == 'med_staff' %}Medical Staff{% else %}Athlete{% endif %}</h1>
    <ol class="breadcrumb mb-4">
      <li class="breadcrumb-item"><a href="{{ path('app_admin_dashboard') }}">Dashboard</a></li>
      <li class="breadcrumb-item"><a href="{{ path('app_user_index', {'context': 'back', 'role': role}) }}">{% if role == 'coach' %}Coaches{% elseif role == 'med_staff' %}Medical Staff{% else %}Athletes{% endif %}</a></li>
      <li class="breadcrumb-item active">Edit</li>
    </ol>
    <div class="card mb-4">
      <div class="card-body">
        {{ include('user/_form.html.twig', {'button_label': 'Update', 'role': role, 'context': 'back'}) }}
      </div>
    </div>
    <div class="d-flex gap-2">
      <a href="{{ path('app_user_index', {'context': 'back', 'role': role}) }}" class="btn btn-outline-secondary">
        <i class="fas fa-arrow-left"></i> Back to List
      </a>
      {{ include('user/_delete_form.html.twig', {'context': 'back'}) }}
    </div>
  </div>
{% endblock %}